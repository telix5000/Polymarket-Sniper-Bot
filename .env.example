# ═══════════════════════════════════════════════════════════
# ⚡ APEX v3.0 - ONE-LINE CONFIGURATION ⚡
# ═══════════════════════════════════════════════════════════

# === REQUIRED ===
PRIVATE_KEY=your_private_key_here
RPC_URL=https://polygon-rpc.com

# === APEX MODE (Choose one) ===
# CONSERVATIVE: 5% positions, 60% max exposure, +12% weekly target
# BALANCED:     7% positions, 70% max exposure, +18% weekly target
# AGGRESSIVE:  10% positions, 80% max exposure, +25% weekly target
APEX_MODE=AGGRESSIVE

# === OPTIONAL ===
# Enable live trading (default: simulation mode)
LIVE_TRADING=I_UNDERSTAND_THE_RISKS

# Cycle interval in milliseconds (default: 5000)
INTERVAL_MS=5000

# Copy trading target addresses (comma-separated)
TARGET_ADDRESSES=0xYourTarget1,0xYourTarget2

# Telegram notifications (optional)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# === ERROR REPORTING (Optional) ===
# Automatically report errors to GitHub Issues for tracking and investigation
# Create a GitHub Personal Access Token with 'repo' scope:
# https://github.com/settings/tokens
# GITHUB_ERROR_REPORTER_TOKEN=ghp_your_token_here

# === VPN CONFIGURATION ===
# For geo-blocked regions, configure WireGuard or OpenVPN.
# Both support either config file paths OR environment variables.

# Note: WIREGUARD_DNS is optional and fully supported in Alpine containers.
# The bot automatically uses PostUp/PostDown scripts instead of resolvconf.
# If you set WIREGUARD_DNS, VPN DNS servers are prepended to Docker's DNS.
# If you omit it, Docker manages DNS automatically (usually sufficient).

# --- WireGuard (Option 1: Config file) ---
# WG_CONFIG=/path/to/wg0.conf

# --- WireGuard (Option 2: Environment variables) ---
# WIREGUARD_ENABLED=true
# WIREGUARD_INTERFACE_NAME=wg0
# WIREGUARD_ADDRESS=10.0.0.2/24
# WIREGUARD_PRIVATE_KEY=your_private_key
# WIREGUARD_MTU=1320
# WIREGUARD_DNS=1.1.1.1           # Optional - Docker manages DNS, but custom DNS is supported
# WIREGUARD_PEER_PUBLIC_KEY=peer_public_key
# WIREGUARD_PEER_PRESHARED_KEY=optional_preshared_key
# WIREGUARD_PEER_ENDPOINT=vpn.example.com:51820
# WIREGUARD_ALLOWED_IPS=0.0.0.0/0,::/0
# WIREGUARD_PERSISTENT_KEEPALIVE=25
# WIREGUARD_FORCE_RESTART=false

# --- WireGuard (Option 3: Full config content) ---
# NOTE: The YAML-style multiline syntax below is for docker-compose.yml or Kubernetes manifests,
#       NOT for this .env file. In Compose/Kubernetes you can use:
#       WIREGUARD_CONFIG: |
#         [Interface]
#         PrivateKey = your_private_key
#         Address = 10.0.0.2/24
#
#         [Peer]
#         PublicKey = peer_public_key
#         Endpoint = vpn.example.com:51820
#         AllowedIPs = 0.0.0.0/0
#
# For a .env file, you CANNOT use YAML block syntax. Either:
#   1) Point to a config file (recommended): WG_CONFIG=/path/to/wg0.conf
#   2) Or inline with escaped newlines (single line):
#      WIREGUARD_CONFIG="[Interface]\nPrivateKey=your_key\nAddress=10.0.0.2/24\n\n[Peer]\nPublicKey=peer_key\nEndpoint=vpn.example.com:51820\nAllowedIPs=0.0.0.0/0"

# --- OpenVPN (Option 1: Config file) ---
# OVPN_CONFIG=/path/to/client.ovpn

# --- OpenVPN (Option 2: Environment variables) ---
# OPENVPN_ENABLED=true
# OPENVPN_CONFIG="full config file contents here (for Docker Compose use YAML multiline syntax)"
# OPENVPN_CONFIG_PATH=/etc/openvpn/client.ovpn
# OPENVPN_USERNAME=your_username
# OPENVPN_PASSWORD=your_password
# OPENVPN_AUTH_PATH=/etc/openvpn/auth.txt
# NOTE: If using OPENVPN_USERNAME/PASSWORD, your OPENVPN_CONFIG must include:
#       auth-user-pass /etc/openvpn/auth.txt
# OPENVPN_EXTRA_ARGS=--verb 3

# --- VPN Bypass (route certain traffic outside VPN) ---
# VPN_BYPASS_RPC=true                    # Route RPC traffic outside VPN for speed (default: true)
# VPN_BYPASS_POLYMARKET_READS=false      # Route read APIs outside VPN (default: false) [CRITICAL FIX]

# ═══════════════════════════════════════════════════════════
# 🎯 APEX v3.0 AUTO-SCALES EVERYTHING!
# ═══════════════════════════════════════════════════════════
#
# Position sizes, exposure limits, and reserves are now calculated
# automatically based on your balance and APEX_MODE.
#
# OLD SYSTEM (v1/v2): Manual USD limits, static percentages
# NEW SYSTEM (v3.0): Intelligent scaling with account tiers
#
# No need to configure:
# - MAX_POSITION_USD (auto-calculated per strategy)
# - MAX_TOTAL_EXPOSURE_USD (auto-calculated from mode)
# - HEDGE_RESERVE_PCT (intelligent reserve calculation)
# - STACK_MAX_USD, HEDGE_MAX_USD, etc. (all auto-scaled)
#
# Everything adapts as your balance grows! 🚀
# ═══════════════════════════════════════════════════════════

# ============================================
# EMERGENCY SELL CONFIGURATION
# ============================================

# Emergency sell mode when balance is critically low
# Options:
#   CONSERVATIVE - Won't sell below 50% of entry price (default)
#   MODERATE     - Won't sell below 20% of entry price
#   NUCLEAR      - Sells at ANY price (⚠️ may result in massive losses)
EMERGENCY_SELL_MODE=CONSERVATIVE

# Balance threshold to activate emergency sells
# Default: 5 (activates when balance < $5)
EMERGENCY_BALANCE_THRESHOLD=5
