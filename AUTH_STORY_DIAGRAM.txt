╔════════════════════════════════════════════════════════════════════════════════════╗
║                     POLYMARKET CLOB WALLET ADDRESS MISMATCH                        ║
║                            Auth Story Diagnostic                                   ║
╚════════════════════════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: PRIVATE KEY → SIGNER ADDRESS                                              │
└────────────────────────────────────────────────────────────────────────────────────┘

    PRIVATE_KEY (env var)
         │
         ├─ ethers.js: new Wallet(privateKey)
         │
         ▼
    0x9B9883152BfFeFB1cBE2A96FC0391537012ee5D1  (SIGNER/EOA)
    ─────────────────────────────────────────
    📍 Derived from private key
    💰 Balance: $0.00 USDC
    🔑 Used for: Signing transactions


┌────────────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: ENVIRONMENT VARIABLES → FUNDER ADDRESS                                    │
└────────────────────────────────────────────────────────────────────────────────────┘

    POLYMARKET_SIGNATURE_TYPE=1 or 2  (env var)
    POLYMARKET_PROXY_ADDRESS=0xFUNDER_ADDRESS  (env var)
         │
         ├─ auth.ts: Reads proxy configuration
         │
         ▼
    0xFUNDER_ADDRESS  (FUNDER/PROXY)
    ────────────────
    📍 Configured in environment
    💰 Balance: $XXX.XX USDC  ← ACTUAL FUNDS ARE HERE!
    🔑 Used for: Providing capital for trades


┌────────────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: AUTH LOGIC → EFFECTIVE ADDRESS (CORRECT)                                  │
└────────────────────────────────────────────────────────────────────────────────────┘

    src/lib/auth.ts (lines 82-84)
    ─────────────────────────────
    const effectiveAddress = 
      signatureType > 0 && funderAddress 
        ? funderAddress    ← Proxy mode: use FUNDER
        : address;         ← EOA mode: use SIGNER

    ✅ CORRECT: state.address = 0xFUNDER_ADDRESS


┌────────────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: BALANCE CHECK → WRONG ADDRESS (BUG!)                                      │
└────────────────────────────────────────────────────────────────────────────────────┘

    src/start.ts (line 429)
    ───────────────────────
    const usdc = await getUsdcBalance(state.wallet);
                                       ^^^^^^^^^^^^
                                       Wallet object for SIGNER

    src/lib/balance.ts (line 14)
    ────────────────────────────
    const balance = await contract.balanceOf(wallet.address);
                                             ^^^^^^^^^^^^^^
                                             SIGNER: 0x9B9883...

    ❌ BUG: Checks SIGNER (0x9B9883...) with $0.00
    ✅ SHOULD CHECK: FUNDER (0xFUNDER...) with $XXX.XX


╔════════════════════════════════════════════════════════════════════════════════════╗
║                              THE PROBLEM                                           ║
╚════════════════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────┐         ┌─────────────────┐
    │   SIGNER        │         │   FUNDER        │
    │   0x9B9883...   │         │   0xFUNDER...   │
    │                 │         │                 │
    │   💰 $0.00      │         │   💰 $500.00    │
    │   USDC          │         │   USDC          │
    └─────────────────┘         └─────────────────┘
            ▲                           │
            │                           │
            │ ❌ CHECKS THIS!           │ ✅ SHOULD CHECK THIS!
            │                           │
    ┌───────┴────────────────────────────┘
    │
    │  getUsdcBalance(wallet)
    │    └─ contract.balanceOf(wallet.address)
    │       └─ Always uses wallet.address = SIGNER
    │
    └─ Result: Balance = $0.00 ❌
       Reality: USDC is in FUNDER address


╔════════════════════════════════════════════════════════════════════════════════════╗
║                              THE FIX                                               ║
╚════════════════════════════════════════════════════════════════════════════════════╝

    BEFORE                                  AFTER
    ──────                                  ─────

    src/lib/balance.ts                      src/lib/balance.ts
    ──────────────────                      ──────────────────
    
    getUsdcBalance(                         getUsdcBalance(
      wallet: Wallet                          wallet: Wallet,
    )                                         address?: string  ← ADD THIS
                                            )
    {                                       {
      ...                                     const checkAddress = 
      balanceOf(wallet.address)                 address || wallet.address;  ← ADD THIS
    }                                         ...
                                              balanceOf(checkAddress)  ← CHANGE THIS
                                            }


    src/start.ts                            src/start.ts
    ────────────                            ────────────
    
    getUsdcBalance(                         getUsdcBalance(
      state.wallet                            state.wallet,
    )                                         state.address  ← ADD THIS
                                            )


    RESULT:                                 RESULT:
    ───────                                 ───────
    Checks wallet.address                   Checks state.address
      = SIGNER (0x9B9883...)                  = FUNDER (0xFUNDER...)
      = $0.00 ❌                              = $500.00 ✅


╔════════════════════════════════════════════════════════════════════════════════════╗
║                           VERIFICATION                                             ║
╚════════════════════════════════════════════════════════════════════════════════════╝

    1. Check configuration:
       $ echo $POLYMARKET_SIGNATURE_TYPE
       → 1 or 2 (proxy/Safe mode)

       $ echo $POLYMARKET_PROXY_ADDRESS
       → 0xFUNDER_ADDRESS

    2. Check signer balance on-chain:
       🔗 polygonscan.com/token/0x2791.../0x9B9883...
       → $0.00 USDC (explains why balance shows $0.00)

    3. Check funder balance on-chain:
       🔗 polygonscan.com/token/0x2791.../0xFUNDER...
       → $XXX.XX USDC (this is where the money is!)

    4. Apply fix and test:
       ✅ Balance: $XXX.XX (now shows funder balance correctly)


╔════════════════════════════════════════════════════════════════════════════════════╗
║                         SUMMARY                                                    ║
╚════════════════════════════════════════════════════════════════════════════════════╝

    ROOT CAUSE:  Balance check queries signer instead of funder in proxy mode
    
    AFFECTED:    Users with POLYMARKET_SIGNATURE_TYPE=1 or 2 and POLYMARKET_PROXY_ADDRESS set
    
    IMPACT:      Balance shows $0.00 → orders fail with INSUFFICIENT_BALANCE_OR_ALLOWANCE
    
    FIX:         Add address parameter to balance functions, pass state.address
    
    FILES:       src/lib/balance.ts (2 functions)
                 src/start.ts (3 call sites)
    
    CONFIDENCE:  99%
    
    STATUS:      ✅ Root cause identified, fix ready to implement

